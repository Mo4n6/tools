# Third-party code

The project licence in [`LICENSE`](LICENSE) covers code written for Mo4n6 Tools. It does not cover third-party code that ships alongside it.

Two separate things are covered below: code **vendored into this repository**, and code that enters only in the built output under `dist/`, which is what GitHub Pages serves.

## Vendored into this repository

| Where | What | Licence |
|---|---|---|
| `src/features/husk/lexer/charTraits.ts`, `src/features/husk/lexer/tokenKind.ts` | Lexer tables generated from the PowerShell parser | MIT |

Husk's tokenizer reproduces the character classification and token tables of Windows PowerShell so that its lexing matches the real thing. Those two files are generated by `scripts/port-char-traits.mjs` and `scripts/port-tokens.mjs` from `src/System.Management.Automation/engine/parser/CharTraits.cs` and `token.cs` in <https://github.com/PowerShell/PowerShell>, which is MIT licensed.

**Portions are Copyright (c) Microsoft Corporation, licensed under the MIT License.** MIT permits this use and permits the derivative to carry the project licence, on the condition that the copyright notice is retained. It is retained in `src/features/husk/attribution.ts`, in the header of each generated file, and here.

The pinned upstream commit and the exact list of ported files are recorded in [`docs/licenses/husk-manifest.json`](docs/licenses/husk-manifest.json). Regenerate the tables rather than editing them by hand, and update the manifest when the pin moves.

No other third-party code is vendored. `public/dead-letter.html` contains none at all, which is why it can carry the project licence in its own header.

## Enters only in the built output

| Where | What | Licence |
|---|---|---|
| `dist/assets/mp3-encoder-*.js` | `lamejs` MP3 encoder | LGPL-3.0 |
| `dist/assets/kokoro-*.js` | `kokoro-js`, `@huggingface/transformers` | Apache-2.0 |
| `dist/assets/onnxruntime-*.js`, `dist/assets/*.wasm` | ONNX Runtime Web and its WebAssembly build | MIT |
| other `dist/assets/*.js` | bundled runtime dependencies | see the tally below |

## The LGPL component needs attention

`lamejs` is **LGPL-3.0**, and it is the one shipped dependency whose terms reach past its own file.

The build already isolates it: `vite.config.ts` routes `lamejs` and the MP3 encoder adapter into a dedicated `mp3-encoder` chunk rather than inlining it across the bundle. That separation is deliberate and should be preserved, because the LGPL expects a recipient to be able to replace the library with their own version.

Anyone distributing a build of this project should be prepared to supply the `lamejs` source on request. It is available at <https://github.com/zhuker/lamejs>.

If MP3 export is ever dropped, or moved entirely onto the `@ffmpeg/ffmpeg` path already present in `src/tts/`, this obligation goes away with it. Note that `@ffmpeg/ffmpeg` is MIT but loads a separate FFmpeg WebAssembly core whose own licence depends on how that core was built; check it before relying on that path.

### One LGPL package in the tree that does not ship

The tally below lists `@img/sharp-libvips-*` as **LGPL-3.0-or-later**. These arrive through `sharp`, a hard dependency of `@huggingface/transformers`, and they are native Node addons that cannot run in a browser.

They do not reach the build. `@huggingface/transformers` resolves to `dist/transformers.web.js` through the `default` export condition, and that file was built with `sharp` stubbed out — it appears in it only as `*** sharp (ignored) ***`. A production build contains no reference to `sharp` and no `libvips` code.

So no LGPL obligation follows from them today. It would follow if the project ever bundled the Node entry point of `@huggingface/transformers`, so this is worth rechecking if that changes.

`dompurify` is dual licensed as MPL-2.0 or Apache-2.0. This project takes it under **Apache-2.0**, so no reciprocal obligation applies.

## Runtime dependency licence tally

Packages reachable from `dependencies` in `package.json`, including transitive and optional ones:

| Licence | Packages | Examples |
|---|---|---|
| MIT | 114 | @asamuzakjp/css-color, @csstools/css-calc, @csstools/css-color-parser, @csstools/css-parser-algorithms, @csstools/css-tokenizer, @ffmpeg/ffmpeg, and 108 more |
| BSD-3-Clause | 15 | @protobufjs/aspromise, @protobufjs/base64, @protobufjs/codegen, @protobufjs/eventemitter, @protobufjs/fetch, @protobufjs/float, and 9 more |
| Apache-2.0 | 11 | @huggingface/transformers, @img/sharp-linux-x64, @img/sharp-linuxmusl-x64, @mozilla/readability, detect-libc, flatbuffers, and 5 more |
| ISC | 7 | @isaacs/fs-minipass, guid-typescript, json-stringify-safe, lru-cache, saxes, semver, and 1 more |
| BlueOak-1.0.0 | 4 | chownr, minipass, tar, yallist |
| BSD-2-Clause | 2 | entities, webidl-conversions |
| LGPL-3.0-or-later | 2 | @img/sharp-libvips-linux-x64, @img/sharp-libvips-linuxmusl-x64 |
| (MIT OR CC0-1.0) | 1 | type-fest |
| (MPL-2.0 OR Apache-2.0) | 1 | dompurify |
| LGPL-3.0 | 1 | lamejs |
| MIT-0 | 1 | @csstools/color-helpers |

Total: 160 runtime package versions, across 159 distinct names. `onnxruntime-common` is installed twice — 1.22.0-dev under `onnxruntime-web` and 1.21.0 under `onnxruntime-node` — and both are MIT. The generator counts each resolved copy rather than each name, because two copies of a package can differ in licence and in what they pull in.

The `@img/sharp-*` platform packages are resolved per platform, so the exact set varies by machine; the counts above come from a Linux x64 install. See the note above for why they do not ship.

Regenerate this tally rather than editing it by hand:

```
npm run licenses          # the markdown table above
npm run licenses -- --json
```

## Models

Model weights are covered separately from code, and neither set is vendored here — both are fetched or supplied at run time.

- Text-to-speech weights for Momoro Reader: [`docs/licenses/tts-manifest.json`](docs/licenses/tts-manifest.json).
- Glass runs ONNX upscaling models that **you** supply. Nothing is bundled, and the licence of a model you load is between you and whoever published it.
- Husk loads no models at all.
